@inherits BaseButton


<ButtonComponent Caption="@(Caption ?? "Ekle")"
				 Color="@(Color ?? "primary")"
				 Icon="@(Icon ?? "plus-square")"
				 OnClick="OnClick"
				 ToolTipStr="@(ToolTipStr ?? "Ekle")"
				 IsOutline="@IsOutline"
				 IsSmall="@IsSmall"
				 buttonType="@ButtonType.Submit"
				 />