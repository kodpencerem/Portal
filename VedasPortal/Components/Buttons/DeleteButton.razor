
@inherits BaseButton


<ButtonComponent Caption="@(Caption ?? "Sil")"
                 Color="@(Color ?? "danger")"
                 Icon="@(Icon ?? "trash-o")"
                 OnClick="OnClick"
                 ToolTipStr="@(ToolTipStr ?? "Sil")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />

