
@inherits BaseButton


<ButtonComponent Caption="@(Caption ?? "Uyarı")"
                 Color="@(Color ?? "danger")"
                 Icon="@(Icon ?? "trash-o")"
                 OnClick="OnClick"
                 ToolTipStr="@(ToolTipStr ?? "Uyarı")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />

