@inherits BaseButton

<ButtonComponent ToolTipStr="@(ToolTipStr ?? "Kaydet")"
                 Color="@(Color ?? "success")"
                 Icon="@(Icon ?? "check")"
                 OnClick="OnClick"
                 Caption="@(Caption ?? "Kaydet")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall"
                 IsBusy="@IsBusy"
                 IsBusyCaption="@(IsBusyCaption ?? "Kaydediliyor")"
                 buttonType="ButtonType.Submit"/>