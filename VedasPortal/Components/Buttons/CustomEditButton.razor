
@inherits BaseButton


<ButtonComponent Caption="@(Caption ?? "Düzenle")"
                 Color="@(Color ?? "secondary")"
                 Icon="@(Icon ?? "pencil-square-o")"
                 OnClick="OnClick"
                 ToolTipStr="@(ToolTipStr ?? "Düzenle")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />

