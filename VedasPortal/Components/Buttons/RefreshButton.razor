
@inherits BaseButton

<ButtonComponent ToolTipStr="@(ToolTipStr ?? "Yenile")"
                 Color="@(Color ?? "success")"
                 Icon="@(Icon ?? "refresh")"
                 OnClick="OnClick"
                 Caption="@(Caption ?? "Yenile")"
                 IsOutline="@IsOutline"
                 IsBusy="@IsBusy"
                 IsBusyCaption="@(IsBusyCaption ?? "Yükleniyor")"
                 IsSmall="@IsSmall" />
