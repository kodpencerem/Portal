@inherits BaseButton

<ButtonComponent ToolTipStr="@(ToolTipStr ?? "Çıkış")"
                 Color="@(Color ?? "danger")"
                 Icon="@(Icon ?? "times")"
                 OnClick="OnClick"
                 Caption="@(Caption ?? "Çıkış")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" 
                 buttonType="ButtonType.Reset"/>