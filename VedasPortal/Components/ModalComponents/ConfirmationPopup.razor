
@inherits BaseModal


<div class="card-body">
    <p>@Message</p>
</div>

<div class="row justify-content-center">

    <div class="d-flex">
        <button class="btn btn-danger" @onclick="@ConfirmClicked">@YesText</button>
        <button class="btn btn-light" @onclick="@CancelClicked">@CancelText</button>
    </div>

</div>

@code {

    public async override Task SetParametersAsync(ParameterView parameters)
    {
        Message = parameters.GetValueOrDefault<String>("Message") ?? "Aradığınız Kriterlerde Bir Veri Bulunumadı!";
        YesText = parameters.GetValueOrDefault<String>("YesText") ?? "Evet";
        CancelText = parameters.GetValueOrDefault<String>("CancelText") ?? "Çıkış";

        await base.SetParametersAsync(parameters);
    }
}
