
<CardComponent cardStyle="Style.warning">
    <CardHeader>
        Öne Çıkan Anket
    </CardHeader>
    <CardBody>
        @if (!isReady)
        {
<p>Yükleniyor...</p> }
else if (Survey != null)
{
<h5 class="card-title">@Survey.SurveyName</h5> {
<table class="table-borderless">
    <tbody>

        @foreach (var option in Survey.SurveyOptions)
        {
<AnketSecenekItem Item="@option" TotalSurveyVotes="@Survey.TotalVotes" />}

    </tbody>
</table> }
<div class="mt-3">
    <button href="#" class="btn btn-outline-dark" @onclick="()=> TakeSurvey(Survey.SurveyId)">Ankete Katıl</button>
</div> }
else
{
<p class="text-center text-danger">Şu anda mevcut anket yok</p>}
    </CardBody>
    <CardFotter>
        @if (Survey != null)
        {
<p>@(Survey == null ? "" : Survey.CreatedOn.ToShortDateString()) tarihinde oluşturuldu.</p> }
else
{
<p></p>}

    </CardFotter>
</CardComponent>



